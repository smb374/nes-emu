[package]
name = "nes-emu"
version = "0.1.0"
edition = "2024"

[dependencies]
bitflags = "^2"
sdl2 = { version = "^0.38", features = ["use-pkgconfig", "static-link", "mixer"] }
rand = "^0.9"
clap = { version = "^4", features = ["derive"] }

[[bin]]
name = "emulator"
path = "src/bin/main.rs"
